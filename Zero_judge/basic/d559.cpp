#include <bits/stdc++.h>
using namespace std;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);

    int n=0;
    while(cin>>n){
        cout<<"\'C\' can use printf(\"\%d\",n); to show integer like "<<n<<"\n";
    }
}